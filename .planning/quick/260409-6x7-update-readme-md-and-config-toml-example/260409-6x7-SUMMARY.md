---
phase: quick
plan: 260409-6x7
subsystem: docs
tags: [documentation, config, readme, read_timeout_ms]
one_liner: "Documented read_timeout_ms and cycle-duration WARN in config.toml.example and README.md"
dependency_graph:
  requires: [01-01]
  provides: [QUICK-6x7]
  affects: [config.toml.example, README.md]
tech_stack:
  added: []
  patterns: []
key_files:
  modified:
    - config.toml.example
    - README.md
decisions:
  - "Added four-line commented block for read_timeout_ms in config.toml.example following existing comment style"
  - "Inserted serial.read_timeout_ms row in README field table immediately after serial.baud_rate for logical grouping"
  - "Added cycle WARN subsection before the closing --- to keep fault/recovery patterns together"
  - "Appended troubleshooting row as last entry in the table without reordering existing rows"
metrics:
  duration: "~1 min"
  completed_date: "2026-04-08"
  tasks_completed: 2
  files_modified: 2
---

# Quick Task 260409-6x7: Update README.md and config.toml.example Summary

## One-Liner

Documented `read_timeout_ms` (optional, default 150 ms) and the poll-cycle-exceeds-interval `WARN` log in both `config.toml.example` and `README.md` to sync docs with Phase 01 poll-speed optimisation.

## What Was Done

Phase 01 shipped `read_timeout_ms` as an optional `[serial]` config key (default 150 ms) and added a `WARN` log when a full poll cycle exceeds `poll_interval_secs`. Neither doc file was updated at the time. This quick task closes that gap.

### Task 1 — config.toml.example

Inserted a four-line block immediately after `baud_rate = 9600` in the `[serial]` section:

```toml
# Modbus read timeout in milliseconds. Default: 150.
# Lower values speed up multi-device polling; raise if devices report sporadic timeouts.
# Energy reset commands always use a fixed 500 ms timeout regardless of this setting.
# read_timeout_ms = 150
```

**Commit:** `58c3248`

### Task 2 — README.md (four targeted edits)

| Edit | Location | Change |
|------|----------|--------|
| 1 | Config block snippet (~line 122) | Added commented `read_timeout_ms` entry after `baud_rate = 9600` |
| 2 | Field reference table (~line 162) | Inserted `serial.read_timeout_ms` row: `u64`, optional, default `150` |
| 3 | Log patterns section (~line 365) | Added cycle-duration WARN subsection with example and explanation |
| 4 | Troubleshooting table (~line 453) | Appended row for `Poll cycle took Xms, exceeds configured interval of Ys` |

**Commit:** `eb2a031`

## Commits

| Task | Commit | Message |
|------|--------|---------|
| 1 | `58c3248` | `docs(quick-260409-6x7): add read_timeout_ms to config.toml.example` |
| 2 | `eb2a031` | `docs(quick-260409-6x7): update README.md with read_timeout_ms and cycle-WARN info` |

## Deviations from Plan

None — plan executed exactly as written.

## Known Stubs

None.

## Self-Check: PASSED

- `config.toml.example` contains `read_timeout_ms`: ✓ (line 25)
- `README.md` contains `read_timeout_ms`: ✓ (lines 126, 167, 375, 467)
- `README.md` contains `Poll cycle took`: ✓ (lines 375, 467)
- Commits `58c3248` and `eb2a031` exist: ✓
